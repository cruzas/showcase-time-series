-r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting and formatting. Pinned exactly: ruff's formatter output changes
# between minor releases, so CI, pre-commit and local runs must agree.
ruff==0.16.5
pre-commit>=3.7
